@* @using MemoryMosaic.Pages *@
@* <link href="css/Components/Import/LibraryItemDisplay.css" rel="stylesheet"/> *@

@* <div class="libraryItem"> *@
	@* <MaterialIcon Icon="check_circle" Class="checkbox unchecked"/> *@
	@* <img src="data:image/jpg;base64,@LibraryItem.thumbnail" onclick="window.open('mm_library/@LibraryItem.path', '_blank').focus();" alt="" loading="lazy"/> *@
	@* <span class="shortPath @(Import.pathWidthClass == "shortWidth" ? "shortWidth" : "fullWidth")">@("Library/" + LibraryItem.path)</span> *@
	@* <span class="dateTaken">@(LibraryItem.dateTaken == null ? "No Date Taken" : LibraryItem.dateTaken)</span> *@
	@* <span class="inTrash">@(LibraryItem.dateDeleted != null ? "In Trash" : "")</span> *@
	@* <IconButton Icon="trash" OnClick="@DeleteBtnClick"/> *@
@* </div> *@

@* @code { *@

	@* [Parameter, EditorRequired] public LibraryItem LibraryItem { get; set; } = null!; *@

	@* [Parameter, EditorRequired] public Action Rerender { get; set; } = null!; *@

	@* private void DeleteBtnClick() *@
	@* { *@
		@* if (LibraryItem.dateDeleted == null) *@
			@* C.MoveToTrash(LibraryItem.Id); *@
		@* C.RemoveFromTrash(LibraryItem.Id); *@
		@* Import.OrganizeItems(); *@
		@* Rerender.Invoke(); *@
	@* } *@
@* } *@