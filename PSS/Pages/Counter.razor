@page "/counter"
@using PSS.Client.Backend

@foreach (var media in mediaList)
{
    <img src=@media.path style="max-width: 10%" />
}

@code {
    public List<Connection.Media> mediaList = LoadMediaTable();

    public static List<Connection.Media> LoadMediaTable()
    {
        Connection c = new Connection();
        List<Connection.Media> l = new();
        l = c.LoadMediaTable();
        @*c.InsertMedia("new path", DateTime.Now);*@
        @*List<Connection.Media> l = c.LoadMediaTable();*@
        return l;
    }
}
