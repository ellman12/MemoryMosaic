@*A blank page just for testing stuff*@
@page "/test"
@using System.IO
@using PSS.Backend

<title>Test</title>

@{
    //Generate dummy data
    // for (int i = 0; i < 40; i++) Connection.InsertMedia("item " + i, DateTime.Now);
    
    // string[] paths = Directory.GetDirectories(PSS.Settings.libFolderFullPath, "*", SearchOption.AllDirectories);
    // paths = paths.Concat(Directory.GetDirectories(PSS.Settings.uploadRootPath, "*", SearchOption.AllDirectories)).ToArray();

    // paths = paths.Reverse().ToArray(); //Start at deepest folder and work up (to avoid dir not found errors).
    // foreach (string path in paths)
        // if (Backend.Maintenance.IsFolderEmpty(path)) Directory.Delete(path, true);
}